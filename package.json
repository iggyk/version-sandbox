{
  "name": "root",
  "version": "1.0.0",
  "description": "This is a sandbox testing project, it does nothing; ignore this please",
  "main": "index.js",
  "scripts": {
    "prep": "node prep.js && git commit -a -m \"test cycle\" && git push",
    "post": "node post.js",
    "version:preminor": "lerna version --conventional-commits --conventional-prerelease",
    "version:minor": "lerna version minor",
    "canary": "lerna publish --canary minor",
    "lerna:publish": "lerna publish from-git",
    "cycle": "npm run prep && npm run version:preminor && npm run post && npm run publish",
    "prepublish": "node post.js"
  },
  "keywords": [
    "sandbox",
    "noop"
  ],
  "author": "iggykogan",
  "license": "ISC",
  "dependencies": {
    "lerna": "^3.22.1",
    "lerna-cli": "^3.0.0"
  }
}
